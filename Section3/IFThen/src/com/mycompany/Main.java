package com.mycompany;

public class Main {

    public static void main(String[] args) {
        // Boolean variable
        boolean isAlien = false;

        // If statement condition testing variable for boolean value
        // == test the operands if they are equal
        // = is the assignment operator which would assign the variable to value following.
        // It is better to have the code within the if statements surrounded in a code block.
        /*
        if(expression) {
            // Code here
        }
         */
        if(isAlien == false) {
            System.out.println("It is not an alien.");
            System.out.println("And I am scare of aliens");
        }

        // If Then statement
    }
}
